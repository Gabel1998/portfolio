CORS_ORIGINS=http://localhost

# Grafana admin password (observability dashboards). Anonymous users get read-only Viewer access.
GRAFANA_ADMIN_PASSWORD=changeme

# Project database passwords (add per project)
# DB_PASSWORD_RAIDFINES=changeme
# DB_PASSWORD_WEBCRAWLER=changeme
