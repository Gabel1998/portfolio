---
# Collections this playbook needs. Install with:
#   ansible-galaxy collection install -r requirements.yml
# (The full `ansible` package already bundles community.general; ansible-core does not.)
collections:
  - name: community.general
