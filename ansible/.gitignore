# Real inventory (may contain host IPs / per-host vars)
inventory.ini

# Ansible scratch
*.retry

# Vault password files / decrypted secrets
.vault_pass
*.vault

# Backup secrets (restic password + Spaces keys) — never commit
secrets.yml
